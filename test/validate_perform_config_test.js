/**
 * Test case for validatePerformConfig.
 * Runs with mocha.
 */
'use strict'

const validatePerformConfig = require('../lib/validating/validate_perform_config.js')
const assert = require('assert')


describe('validate-perform-config', function () {
  this.timeout(3000)

  before(() => {

  })

  after(() => {

  })

  it('Validate perform config', () => {

  })
})

/* global describe, before, after, it */
