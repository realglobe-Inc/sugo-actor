/**
 * Test case for validatePerformConfig.
 * Runs with karma.
 */
'use strict'

const validatePerformConfig = require('../shim/browser/validating/validate_perform_config.js')
const assert = require('assert')


describe('validate-perform-config', function () {
  this.timeout(3000)

  before(() => {

  })

  after(() => {

  })

  it('Validate perform config', () => {

  })
})

/* global describe, before, after, it */
