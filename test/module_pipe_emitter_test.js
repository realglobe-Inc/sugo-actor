/**
 * Test case for modulePipeEmitter.
 * Runs with mocha.
 */
'use strict'

const modulePipeEmitter = require('../lib/emitting/module_pipe_emitter.js')
const assert = require('assert')
const co = require('co')

describe('module-pipe-emitter', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Module pipe emitter', () => co(function * () {

  }))
})

/* global describe, before, after, it */
